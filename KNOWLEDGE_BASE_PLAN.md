# ChatLab AI 知识库实现计划

- 开发分支：`feat/ai-knowledge-base`
- 调研基线：`4392519d303cc58eddc9145fec2dcba6e0376283`，仓库版本 `0.37.0`
- 制定日期：2026-09-08
- 状态：计划已制定，以下功能与验收项尚未实现或执行。
- 首版目标：导入聊天 → AI 提炼、分类、关联与去重 → 本地知识库 → Obsidian / Notion 增量输出。

## 1. 产品目标与首版范围

用户选择需要整理的聊天和时间范围，AI 自动生成可长期积累的知识条目。首页展示“新增了什么、更新了什么、哪些内容存在疑问”，让整理成果可以直接阅读和使用。

首版支持概念解释、操作教程、资源推荐、方案比较、经验记录五类内容。每条知识包含标题、摘要、正文、主题、标签、原文出处和更新时间；可以跨群、跨日期关联同一主题。原文中的观点、经验和推断保留归属，不自动提升为已经验证的事实。

默认决策：

| 项目 | 首版选择 |
| --- | --- |
| 核心交互 | 选择资料后让 AI 整理；浏览、编辑和检索结果 |
| 知识主存储 | ChatLab 本地独立知识数据库 |
| 运行平台 | Electron Desktop 和 CLI Web 共享实现 |
| AI 模型 | 复用应用现有模型配置、调用链和用量记录 |
| Obsidian | 输出到用户指定 Vault 子目录；支持再次更新 |
| Notion | 输出到用户指定的数据源；支持再次更新 |
| 同步方向 | ChatLab → Obsidian / Notion |
| 检索 | 先交付中英文关键词搜索，再接入带出处的知识问答 |
| 自动运行 | 用户启用后，在导入完成或手动刷新时整理增量内容 |
| 后续扩展 | Obsidian / Notion 资料导入、知识语义检索、受控双向同步 |

首版以文字聊天和消息中的 URL 为输入。链接先作为资源保存，网页全文抓取、PDF/OCR、语音理解和任意文件索引列入后续范围。纯浏览器 Web WASM 首版不开放依赖 Node、文件系统和凭证的知识库功能，其现有流程需要通过回归检查。

## 2. 已核对的代码基础

| 已有能力 | 代码入口 | 实施判断 |
| --- | --- | --- |
| 会话、消息和统一数据库接口 | `packages/core/src/schema/tables.ts`、`packages/core/src/interfaces/database-adapter.ts` | 通过既有 adapter 读取来源，不重复实现导入器 |
| 按天归纳话题 | `packages/node-runtime/src/services/topics/` | 已有分块、证据、来源签名、暂停恢复、执行租约与用量统计；优先复用适用部分 |
| 模型调用 | `packages/node-runtime/src/services/topics/model-client.ts`、`packages/node-runtime/src/ai/` | 复用 `buildPiModel` / `completeSimple`；知识提炼使用独立输出协议 |
| AI 记忆 | `packages/node-runtime/src/ai/memory.ts` | 当前用于有限长度的记忆注入；知识条目需要独立持久化、版本和同步记录 |
| 聊天语义索引 | `packages/node-runtime/src/semantic-index/` | 已有 embedding 和证据检索；知识条目索引需要适配自身 ID 和内容版本 |
| 跨端 HTTP 服务 | `packages/http-routes/src/routes/web/ai-chat-topics.ts` | Electron 和 CLI Web 可共用 route / service |
| 前端 service 注册 | `src/services/registry.ts`、`src/services/chat-topics/` | 两端现有话题功能均使用 Fetch adapter，新功能沿用此模式 |
| 页面路由 | `src/routes/routes.ts` | 增加知识库页面与入口，接入现有应用布局 |
| 凭证存储 | `packages/config/src/auth-profiles.ts` | 已有凭证与一般配置分离、文件锁、原子写入；集成凭证采用独立 profile |
| 已安装依赖 | `packages/node-runtime/package.json` | SQLite、gray-matter、undici 和模型运行时均可复用 |

实现前继续遵守根目录 `AGENTS.md` 和 `docs/cn/contributing/development.md`。公开分支没有额外的嵌套 `AGENTS.md` 或 `.docs/` 开发上下文。本计划不依赖私有文档。

## 3. 技术结构和数据约定

沿用当前 TypeScript、Vue 3、Electron、Fastify 和 SQLite 结构。

| 层 | 计划新增或扩展的位置 | 职责 |
| --- | --- | --- |
| 共享类型 | `packages/shared-types/knowledge-base.ts` | 条目、来源、任务、同步状态和请求/响应类型 |
| 平台无关数据逻辑 | `packages/core/src/knowledge-base/` | Schema、查询、版本及合并规则 |
| Node 业务服务 | `packages/node-runtime/src/services/knowledge-base/` | 数据库生命周期、AI 整理任务、导出和同步 |
| 共享 API | `packages/http-routes/src/routes/web/knowledge-base.ts` | 参数校验、认证边界、响应和错误映射 |
| 前端访问 | `src/services/knowledge-base/` | Fetch adapter，注册至现有 service registry |
| 产品界面 | `src/pages/knowledge-base/`、`src/components/knowledge-base/` | 列表、详情、筛选、整理任务和集成配置 |
| AI 工具 | `packages/tools/` 中的现有工具注册位置 | 知识搜索和条目读取，接入现有 Agent |

这些是拟定路径；只在对应阶段创建，按实际职责合并小文件，避免提前建设通用插件或任务框架。Route/IPC 不直接承载业务 SQL。

知识库存放在通过 `PathProvider` 解析的 `<userDataDir>/knowledge-base/knowledge.sqlite`。跨聊天来源使用全局知识 ID 关联，避免知识被锁定在单个聊天库中。

按阶段增加以下数据：

| 数据 | 关键字段与用途 |
| --- | --- |
| 知识条目 | UUID、标题、摘要、Markdown 正文、类型、主题、标签、状态、版本号、创建/更新时间 |
| 来源引用 | 知识 ID、sessionId、messageId、平台消息 ID（如有）、时间、片段、内容指纹、来源状态 |
| 条目关系 | 两个知识 ID、关系类型（相关/补充/冲突）、依据 |
| 版本记录 | 修改前后内容、产生方式（AI/用户/合并）、对应任务和来源 |
| 整理任务与检查点 | 来源范围、分块签名、模型/提示词版本、已完成位置、用量、错误、运行状态 |
| 同步记录与操作状态 | 目标 ID、知识 ID、本地版本、外部文件或页面 ID、上次成功内容指纹、失败/冲突状态 |

关键约定：

1. 来源身份至少使用 `sessionId + messageId`；消息 ID 不能跨聊天单独使用。重导入、会话合并或删除后重新核对来源，缺失引用明确显示“原始来源已不可用”。
2. 聊天数据库的 `ts` 为秒，既有证据 payload 为毫秒。新 API 统一毫秒，在读取聊天库的边界转换，导出使用明确时区的日期文本。
3. AI 返回值使用结构化协议并在服务端校验；引用 ID 必须来自本次读取的消息。无依据的内容不作为正常知识直接入库。
4. 通过校验的条目自动保存；低把握合并、矛盾或需要补充依据的结果进入“待处理”，不要求用户逐条批准全部正常产出。
5. 同一来源和提炼版本重复运行应复用已有成果；新证据追加到已有条目，内容修订保留版本。相似标题不构成合并的充分理由。
6. 用户编辑后的正文不能被后续 AI 整理静默覆盖；新建议与已有版本比较，冲突交给用户选择。
7. 独立数据库使用事务和显式 schema 版本。是否提升数据目录兼容门禁，以旧版 runtime 实际能否安全读写为依据；不任意改写现有版本号或聊天库结构。

## 4. 按顺序实施的里程碑

### M0：建立可复现的开发基线

- 切换到功能分支，核对分支头与本计划基线的差异。
- 按仓库要求使用 Node 24、pnpm 11 和现有 lockfile 安装依赖。
- 跑相关现有测试及类型检查，记录环境、命令和基线问题。
- 准备合成聊天样例，包含技术讨论、重复消息、互相矛盾的说法、链接及无意义消息。

交付：可以启动的开发环境、可重复使用的样例、基线检查记录。

### M1：本地知识库可以使用

- 建立独立数据库、来源引用与条目版本。
- 新增知识库入口，支持手动建条目、编辑、归档、恢复、列表筛选和关键词搜索。
- 在聊天记录中提供“保存为知识”，支持选择消息并保留出处。
- 点击出处可回到相应聊天、时间和消息；找不到原文时保留清晰状态。
- 增加共享 HTTP route、Fetch service 与对应多语言文案。

验收：选择几条消息保存为知识，重启应用后仍可检索、编辑和回到原文；Desktop 与 CLI Web 返回一致的数据结构。

### M2：AI 自动整理聊天

- 增加“整理为知识”流程：选择聊天、时间范围和关注主题，展示预估处理量。
- 优先复用已有话题结果进行组织；仍读取被引用的原文，避免只对摘要再次摘要而丢失依据。没有话题结果时直接分块提炼，不强制额外跑一遍付费摘要。
- 复用适用的分块、模型调用、来源签名和任务控制代码；知识任务保存自己的检查点。
- 提取结构化知识，过滤无意义内容，生成标签、资源链接和关联建议。
- 先做内容/来源精确去重，再检索少量已有知识作为语义合并候选；矛盾内容保留各自来源。
- 提供任务进度、暂停、继续、取消、失败重试和用量汇总。默认限制后台并发并让交互式 AI 请求优先。
- 运行完成展示新增、更新、跳过、待处理的数量和对应条目。

验收：同一批数据再次整理不产生重复条目；中断后能从已落盘的检查点继续；追加旧日期消息也能被来源变化检测发现；伪造引用、无效 JSON 和上下文中的指令不能污染知识库。

说明：进程在模型已计费、结果尚未落盘时崩溃，恢复可能需要重新请求该块；计划保证已持久化检查点不重复执行，不承诺外部计费“恰好一次”。

### M3：Obsidian 集成

- 在桌面端选择 Vault，默认写入专用 `ChatLab/` 子目录。
- 每条知识生成 Markdown 笔记，包含正文、来源、相关知识链接和 YAML 属性。
- 使用稳定知识 ID 标识笔记，首次生成安全文件名并记录映射；标题变化不会额外生成一份笔记。
- 生成主题索引页，并仅对实际存在的目标笔记写入内部链接。
- 再次输出只更新变化条目；保留用户自定义属性和人工补充区域。
- 写入前比较上次生成内容与当前文件。人工改动生成区域、文件被移动或目标冲突时展示处理选项；使用临时文件与原子替换避免半写文件。
- CLI Web 的路径属于服务所在机器。同机部署可写配置目录；远端部署先提供 Markdown 下载，批量打包在确认可复用依赖后交付。

验收：Obsidian 正确显示中文标题、标签和链接；重复输出不重复建文件；包含 Windows 非法文件名字符的标题可处理；人工笔记得到保留；输出路径不能越出配置目录。

格式依据：[Obsidian 属性](https://obsidian.md/help/properties)使用文件头部 YAML；[内部链接](https://obsidian.md/help/links)支持 Wikilink 和 Markdown 链接，路径与特殊字符需按其规则处理。

### M4：Notion 集成

- 设置中配置独立集成凭证和目标数据源，提供连接检查与字段映射。
- 复用现有 auth profile 的保存机制，使用明确的 Notion profile 名称；凭证不写入知识正文、浏览器持久状态、日志或导出文件。
- 在 Node 侧调用官方 REST API，固定经过契约测试的 `Notion-Version`。本次核对官方文档版本为 `2026-03-11`，按 database/data source 分离模型处理。
- 每条知识对应一个页面；属性至少包括标题、ChatLab ID、类型、标签和更新时间，正文包含摘要、正文与出处。
- 保存 `targetId + knowledgeId → pageId` 映射和同步版本，不通过页面标题识别同一条知识。
- 再次同步按 ID 更新；更新页面属性和正文都需完成，不能只更新数据库列。
- 将应用生成内容放在可识别的区域，记录受管 block ID 与内容指纹，保留用户在其他区域新增的内容；受管区域被人工修改时报告冲突。
- 持久化待同步版本和每步进度；创建请求结果不明时先按 ChatLab ID 查询，不能盲目再次创建。发现多个匹配时提示修复，避免继续制造重复。
- 处理分页、正文分段、限流和有限重试；无权限、凭证失效、目标被删除时保留待同步状态并给出可执行错误信息。
- 首版的删除只归档本地条目，外部副本保留；需要删除外部副本时提供单独的明确操作。

验收：同一知识连续同步两次只有一个页面；修改正文会更新原页面；模拟超时、部分成功和限流后可以恢复；用户补充不被静默覆盖。

API 依据：[版本管理](https://developers.notion.com/reference/versioning)、[数据库与 data source](https://developers.notion.com/guides/data-apis/working-with-databases)、[请求限制](https://developers.notion.com/reference/request-limits)。请求队列遵守 `Retry-After`，对非幂等写入先核对执行结果再决定重试。

### M5：增量积累与基于知识的 AI 分析

- 在用户开启自动整理后，导入完成将变化范围加入知识任务；保存配置和任务，重启后可继续处理。
- 增量识别依据来源变化而不只依赖最大时间戳，覆盖补录历史聊天的情况。
- 增加按主题聚合的知识浏览和“这次新增/修正了什么”视图。
- 为现有 Agent 增加知识搜索与读取工具，问答引用知识条目，并可进一步回到聊天来源。
- 默认允许知识检索工具读取用户选定范围；写入、合并及对外同步仍走显式的知识业务操作。
- 没有匹配知识时明确说明证据不足，不用最近条目填充出似乎相关的回答。
- 自动同步作为用户可开启的选项，仅处理变更条目，展示失败和冲突状态。

验收：追加一批聊天后，只处理发生变化的范围；生成的知识在两个集成目标中可更新；关闭应用时不声称后台任务仍会运行。

**V0.1 完成线：M0–M5 全部完成，并通过一次真实数据流的显式冒烟验证。** M2 完成时先交付可用的 AI 知识库，M3 和 M4 分别交付一个完整的集成闭环。

## 5. 后续版本

| 版本方向 | 功能 | 开始条件 |
| --- | --- | --- |
| V0.2 输入扩展 | 导入用户选择的 Obsidian Markdown 和 Notion 页面，统一为可追溯来源 | V0.1 的条目身份和单向更新稳定 |
| V0.2 检索增强 | 复用现有 embedding 运行时，为知识条目建立按版本更新的语义索引 | 实测关键词检索存在明显漏召回 |
| V0.2 多来源整理 | 聊天、笔记和页面共同分类、去重、关联、发现矛盾 | 来源类型和内容更新语义已完成验证 |
| 后续双向同步 | 外部修改回流、冲突比较、删除与移动处理 | 已明确各平台的编辑归属和恢复策略 |

双向同步会把外部用户修改纳入主数据写入，需单独设计内容版本、回流去重和冲突处理，不把它伪装成“定时覆盖”。

## 6. 验证与完成标准

测试围绕会导致用户丢数据、重复数据或获得错误结果的行为。

| 风险 | 必须覆盖的验证 |
| --- | --- |
| 升级破坏数据 | 新库初始化、升级事务失败回滚、现有聊天库可继续读写；必要时验证数据目录兼容门禁 |
| 知识重复或合并错误 | 重复输入、跨群同名但不同含义、相同消息的新增证据、相互矛盾的内容 |
| 失去出处 | 引用 ID 校验、秒/毫秒转换、原会话删除和重新导入后的来源状态 |
| AI 任务无法恢复 | 暂停/取消、重启检查点、输入变化、两个 runtime 对同一任务的并发争用 |
| 搜索不可用 | 中文短词、英文词、C++ 等技术名、URL，以及无匹配结果 |
| 文件覆盖 | 重复导出、人工修改、Unicode 和非法文件名字符、目录边界、写入中断 |
| Notion 重复或部分写入 | 创建响应丢失、正文分段部分成功、重复点击同步、分页、429/529、认证失效、冲突 |
| 跨端行为分歧 | Desktop 与 CLI Web 的共享 service/route 契约；Web WASM 不误加载 Node 模块 |
| 凭证泄漏 | 配置读取接口只返回脱敏状态，日志与导出不含密钥，Notion profile 不干扰模型配置 |

实施时使用仓库现有检查入口：

- 相关测试：`pnpm test -- path/to/changed.test.ts`。
- 后端类型检查：`pnpm run type-check:node`。
- 前端类型检查：`pnpm run type-check:web`。
- 跨端交付：`pnpm run type-check:all`。
- 定向 lint/format：`pnpm exec eslint <changed-files>`、`pnpm exec prettier --write <changed-files>`。
- 提交前：`git diff --check`；如修改文档站页面，执行 `pnpm docs:build`。
- 最后用合成数据完成导入 → 提炼 → 检索 → Obsidian → Notion → 增量更新的流程验证。默认测试不调用真实模型或真实 Notion。
- 真实模型、Vault 和 Notion 冒烟验证在目标已配置后进行，单独记录运行结果；缺少连接时明确标记“未验证”，不影响先完成本地与模拟验证。

验收数据全部使用合成或明确用于验证的材料；仓库中不提交聊天记录、个人笔记或凭证。

## 7. 开始实施时的第一批任务

- [ ] M0：核对分支差异并建立基线。
- [ ] M1：新增共享知识类型、独立数据库和真实临时 SQLite 测试。
- [ ] M1：实现带出处的条目 CRUD、版本保护和关键词查询。
- [ ] M1：接入共享 HTTP route 与前端 Fetch service。
- [ ] M1：交付知识库页面和“选中消息 → 保存为知识”。
- [ ] M2：先让一个聊天、一个小时间范围完成 AI 自动提炼，再扩展批量和恢复。
- [ ] M3：打通一个临时 Vault 的增量输出。
- [ ] M4：打通模拟 Notion 的创建、更新和失败恢复。
- [ ] M5：联通导入增量触发与带来源的知识问答。
- [ ] 完成真实目标冒烟验证，并在分支中记录每个里程碑的结果。

依赖、lockfile、版本号和发布流程沿用当前仓库规则；预计优先使用已安装依赖。若实现证明必须改动这些受约束项目，再列出具体差异和原因处理。本计划本身只新增规划文档。
